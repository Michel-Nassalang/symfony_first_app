{% extends 'base.html.twig' %}

{% block title %}EtatProduit{% endblock %}

{% block body %}
    <h1>EtatProduit</h1>

    <table id="example" class="display" style="width:100%">
        <tbody>
            <tr>
                <th>Code</th>
                <td>{{ etat_produit.code }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ etat_produit.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_etat_produit_index') }}">back to list</a>

    <a href="{{ path('app_etat_produit_edit', {'id': etat_produit.id}) }}">edit</a>

    {{ include('etat_produit/_delete_form.html.twig') }}
{% endblock %}
