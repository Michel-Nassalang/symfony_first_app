{% extends 'base.html.twig' %}

{% block title %}Vente{% endblock %}

{% block body %}
    <h1>Vente</h1>

    <table id="example" class="display" style="width:100%">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vente.id }}</td>
            </tr>
            <tr>
                <th>Somme</th>
                <td>{{ vente.somme }}</td>
            </tr>
            <tr>
                <th>String</th>
                <td>{{ vente.string }}</td>
            </tr>
            <tr>
                <th>Vendeur</th>
                <td>{{ vente.vendeur }}</td>
            </tr>
            <tr>
                <th>Datetime</th>
                <td>{{ vente.datetime ? vente.datetime|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Produit</th>
                <td>{{ vente.produit }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_vente_index') }}">back to list</a>

    <a href="{{ path('app_vente_edit', {'id': vente.id}) }}">edit</a>

    {{ include('vente/_delete_form.html.twig') }}
{% endblock %}
